# This directory will store CSV data files
# The .gitkeep file ensures the directory exists in the repository
# while actual CSV files may be excluded by .gitignore only sample dataset will not be ignored
